import { Container } from "@/shared/ui/Container";
import styles from "./CatalogProducts.module.scss";

function CatalogProducts() {
  return (
    <section className={styles.section}>
      <Container>dsa</Container>
    </section>
  );
}

export default CatalogProducts;
